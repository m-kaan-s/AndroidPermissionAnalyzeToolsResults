Here are some details about files;

* Previous folder includes first version of small data set and big data set evaluation.
* Root folder includes up-to-date versions of comparision results. Updates are below:
	For small data set; READ CALENDAR test now uses a data set of 200 items instead of 182 items. So, all permission test sets in small data set uses equal amount of items now( 200 items ).
	For big data set; Disagreed applications( When compared with Autocog results ) in the small data set are also included in big data set tests. This will allow a better comparison of small and larger data set test results.
	
